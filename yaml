title: My Site
description: My awesome site.
baseurl: "/contest-and-context" # leave empty "" if at root (https://username.github.io)
url: "https://username.github.io"

theme: jekyll-theme-primer

# Markdown processor
markdown: kramdown
kramdown:
  input: GFM

# Plugins
plugins:
  - jekyll-seo-tag
  - jekyll-paginate
  - jekyll-feed
  - jekyll-sitemap

# Pagination settings
paginate: 10
paginate_path: "/page:num/"

# Permalink structure
permalink: /:title/
